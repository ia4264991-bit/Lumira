<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only network security config: permits plaintext HTTP to the local
  emulator loopback alias so BACKEND_BASE_URL (10.0.2.2) works against a
  backend running on the developer's machine. This file is NOT included in
  release builds (see src/debug vs src/main source sets), so release builds
  remain HTTPS-only via the platform default.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
