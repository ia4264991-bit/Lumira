/**
 * Spring Framework configuration and bean wiring that applies
 * application-wide (e.g. CORS, OpenAPI/Swagger, {@code ObjectMapper}
 * customization) rather than to any single business module.
 *
 * <p>Does NOT hold business logic — a class in this package should wire
 * beans and settings, not implement behavior.
 */
package com.lumira.backend.config;
